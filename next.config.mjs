/** @type {import('next').NextConfig} */

const nextConfig = {
  images: {
    domains: [
      'www.udiscovermusic.com',
      'images.unsplash.com',
      'f4.bcbits.com',
      'i.scdn.co',
      'mir-s3-cdn-cf.behance.net'
    ],
  }
}

export default nextConfig;
